#!/usr/bin/env ruby

require_relative '../lib/studio_ghibli'
#require 'json'
#require 'httparty'
#require 'restclient'
#CLI.new.start
StudioGhibli.new.start

#this is where everything will hit first then everything else will load after. The doorway, basically.
#require keyword because it will be external